### 枚举的值设置
```
enum FooBar {
    Foo,
    Bar,
}

fn main(){
    let a = FooBar::Foo;
    let b = FooBar::Bar;

    match (a, b) {
        (FooBar::Foo,FooBar::Bar) =>{
            println!("{}","一样");
        },
        _ =>{
            println!("{}","onede")
        }
    }
}
```
